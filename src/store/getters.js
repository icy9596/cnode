let getters = {
    topicId (state) {
        return state.topicId;
    },
    token (state) {
        return state.token;
    },
    indexPath (state) {
        return state.indexPath;
    }
};

export default getters;
