let getters = {
    topicId (state) {
        return state.topicId;
    },
    token (state) {
        return state.token;
    },
    indexPath (state) {
        return state.indexPath;
    },
    user (state) {
        return state.user;
    }
};

export default getters;
